{
  "name": "excelsior-cleaning",
  "version": "1.0.0",
  "description": "Excelsior Cleaning Services Website",
  "scripts": {
    "build": "cd functions/get-available-dates && npm install && cd ../manage-dates && npm install"
  },
  "dependencies": {
    "mongodb": "^5.7.0"
  }
}
